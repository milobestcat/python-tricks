### open a jupyter notebook script and type in the following command
%%writefile filename.py
print 'hello world'
### after this, a .py file named filename.py will be automatically created.


### to run the .py file:
% run filename.py


### to remove the filename.py
import os
os.remove('filename.py')


clean up notebook:
1. go to the directory whre file is saved
2. jupyter nbconvert --clear-output --inplace file_name.ipynb

delete non-empty folder:

1. import shutil
2. shutil.rmtree('/folder_name', ignore_errors=True)
